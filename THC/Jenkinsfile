node {
    stage("clean dir"){
    deleteDir()
    }
    stage("Run THC") {
        try {
            sh "docker-compose down"
            sh "docker system prune"
            sh "docker-compose up --build -d"
        } catch (e) {
            error "Service update failed"
        }
    }
}